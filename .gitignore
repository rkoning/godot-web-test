# Godot 4+ specific ignores
.godot/
.nomedia

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg
*.tmp

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Local web export output (CI produces this fresh on every run)
/build/

# NOTE: export_presets.cfg is intentionally tracked - CI cannot export without it.
